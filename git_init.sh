#!/bin/bash
cd ~/ml-orchestration
git init
git add .
git commit -m "Initial commit: ML agent orchestration pipeline"
echo "Git init done. Run: gh repo create Shima808/ml-orchestration --public --source . --remote origin --push"
